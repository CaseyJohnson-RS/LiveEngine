class DomainError(Exception):
    """Базовая доменная исключение."""


class InvariantViolationError(Exception):
    """Нарушен внутренний инвариант — баг в коде, а не отклонённое доменное действие.

    Намеренно не наследуется от DomainError: код, ловящий DomainError, чтобы
    вежливо отклонить нелегальную команду клиента, не должен иметь шанс
    случайно проглотить эту ошибку — она означает, что баг не у клиента,
    а где-то в самом движке.
    """
